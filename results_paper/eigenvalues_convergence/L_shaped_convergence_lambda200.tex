% Convergence of the two hundredth eigenvalue against the degrees of freedom,
% L-shaped domain: the eigenvalue itself on the left, the relative error on the
% right.
%
% Requires in the preamble:
%   \usepackage{graphicx}
%   \usepackage{subfig}
%   \usepackage{epstopdf}   % the figures are EPS; needed under pdflatex,
%                           % which then wants -shell-escape. Not needed for
%                           % latex/dvips, which reads EPS directly.
%
% The figures live next to this file, in
% results_paper/eigenvalues_convergence/. If you \input this snippet
% from elsewhere, point graphicx at that folder, e.g.
%   \graphicspath{{results_paper/eigenvalues_convergence/}}
% and drop the leading path from the \includegraphics arguments below.

\begin{figure}[htbp]
  \centering
  \subfloat[Eigenvalue $\lambda_{200}$.\label{fig:convergence_L_shaped_lambda200_value}]{%
    \includegraphics[width=0.45\textwidth]{L_shaped_convergence_lambda200}}
  \qquad
  \subfloat[Relative error $\frac{|\lambda_{200} - \lambda_{200}^{\text{DST}}|}{\lambda_{200}^{\text{DST}}}$.\label{fig:convergence_L_shaped_lambda200_error}]{%
    \includegraphics[width=0.45\textwidth]{L_shaped_convergence_lambda200_error}}
  \caption{The two hundredth eigenvalue $\lambda_{200}$ of Laplace operator, zero Dirichlet boundary conditions, L-shaped domain \texttt{L\_shaped}. Eigenvalue $\lambda_{200}$ computed by each discretisation \texttt{DST}, \texttt{FD} and \texttt{FEM} with varying degrees of freedom; compared with the reference eigenvalue $\lambda_{200}^{\text{DST}}$ computed by \texttt{DST} with 19845 degrees of freedom.}
  \label{fig:convergence_L_shaped_lambda200}
\end{figure}
