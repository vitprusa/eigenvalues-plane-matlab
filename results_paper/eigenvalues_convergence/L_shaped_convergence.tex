% Convergence against the degrees of freedom, L-shaped domain, three indices in
% one float: a row per eigenvalue, the eigenvalue itself on the left and the
% relative error on the right.
%
% Requires in the preamble:
%   \usepackage{graphicx}
%   \usepackage{subfig}
%   \usepackage{epstopdf}   % the figures are EPS; needed under pdflatex,
%                           % which then wants -shell-escape. Not needed for
%                           % latex/dvips, which reads EPS directly.
%
% The figures live next to this file, in
% results_paper/eigenvalues_convergence/. If you \input this snippet
% from elsewhere, point graphicx at that folder, e.g.
%   \graphicspath{{results_paper/eigenvalues_convergence/}}
% and drop the leading path from the \includegraphics arguments below.
%
% This snippet holds the same six figures as L_shaped_convergence_lambda1.tex,
% _lambda200.tex and _lambda1000.tex, in one float instead of three. Its labels
% carry "combined", so the two forms can be \input into the same document
% without clashing -- useful while deciding which of them the article takes.

\begin{figure}[htbp]
  \centering
  \subfloat[Eigenvalue $\lambda_1$.\label{fig:convergence_L_shaped_combined_lambda1_value}]{%
    \includegraphics[width=0.45\textwidth]{L_shaped_convergence_lambda1}}
  \qquad
  \subfloat[Relative error $\frac{|\lambda_1 - \lambda_1^{\text{MPS}}|}{\lambda_1^{\text{MPS}}}$.\label{fig:convergence_L_shaped_combined_lambda1_error}]{%
    \includegraphics[width=0.45\textwidth]{L_shaped_convergence_lambda1_error}}
  \\
  \subfloat[Eigenvalue $\lambda_{200}$.\label{fig:convergence_L_shaped_combined_lambda200_value}]{%
    \includegraphics[width=0.45\textwidth]{L_shaped_convergence_lambda200}}
  \qquad
  \subfloat[Relative error $\frac{|\lambda_{200} - \lambda_{200}^{\text{DST}}|}{\lambda_{200}^{\text{DST}}}$.\label{fig:convergence_L_shaped_combined_lambda200_error}]{%
    \includegraphics[width=0.45\textwidth]{L_shaped_convergence_lambda200_error}}
  \\
  \subfloat[Eigenvalue $\lambda_{1000}$.\label{fig:convergence_L_shaped_combined_lambda1000_value}]{%
    \includegraphics[width=0.45\textwidth]{L_shaped_convergence_lambda1000}}
  \qquad
  \subfloat[Relative error $\frac{|\lambda_{1000} - \lambda_{1000}^{\text{DST}}|}{\lambda_{1000}^{\text{DST}}}$.\label{fig:convergence_L_shaped_combined_lambda1000_error}]{%
    \includegraphics[width=0.45\textwidth]{L_shaped_convergence_lambda1000_error}}
  \caption{Eigenvalues $\lambda_1$, $\lambda_{200}$ and $\lambda_{1000}$ of Laplace operator, zero Dirichlet boundary conditions, L-shaped domain \texttt{L\_shaped}. Eigenvalues computed by each discretisation \texttt{DST}, \texttt{FD} and \texttt{FEM} with varying degrees of freedom. Reference value for $\lambda_1$ is the value obtained by the method of particular solutions (\texttt{MPS}), reference values for $\lambda_{200}$ and $\lambda_{1000}$ are the values obtained by \texttt{DST} with 19845 degrees of freedom.}
  \label{fig:convergence_L_shaped}
\end{figure}
